import type { NextConfig } from "next";

// Force Next.js server module cache refresh for updated Prisma client
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

